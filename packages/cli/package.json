{
  "name": "motif",
  "version": "0.1.0",
  "description": "Unification layer for AI coding agent sessions — collect, search, and natively hand off sessions across tools. Self-hosted.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "motif": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@motif/core": "*",
    "@motif/server": "*",
    "commander": "^12.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
